# docs

What's tracked here:

- `community-wall-schema.sql` — Supabase migration for the community wall (`/community-wall`)
- `reactions-schema.sql` — Supabase migration for the blog-post reaction picker (`/api/reactions`)

Run either against your Supabase project's SQL editor (Dashboard → SQL Editor → New query → paste → Run) to create the tables, indexes, RLS policies, and aggregation views.

The rest of the planning / research / audit docs that used to live here are gitignored — useful as my internal context, not interesting enough to publish.
